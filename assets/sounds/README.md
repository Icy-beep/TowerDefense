# Звуки

Чтобы добавить звук, положи `.wav`, `.ogg` или `.mp3` файл в нужную папку — он подхватится
автоматически при следующем запуске игры, код трогать не нужно.

Если в папке несколько файлов, при каждом проигрывании выбирается случайный — удобно
для вариаций одного и того же звука (например, три разных выстрела лазера).

Звуки, привязанные к точке на карте (выстрелы, взрыв, постройка, смерть врага),
приглушаются вне текущего вида камеры и полностью пропадают далеко за его пределами.
Предупреждение об атаке базы (`base_hit`) — исключение: играет на полной громкости всегда,
даже если камера смотрит в другое место, ведь это предупреждение, а не боевой фидбек.

Каждое проигрывание также получает небольшую случайную вариацию питча (±6%, `SoundManager.PITCH_VARIATION`),
чтобы частые повторы одного и того же файла (например пулемётные выстрелы или лазерные попадания)
не звучали монотонно. Вариации питча просчитываются заранее один раз при загрузке звука, а не на
каждый `play()` — иначе частая стрельба (лазер, пулемёт) вызывала статтер из-за ресемплинга на лету.
Ничего для этого делать не нужно — работает автоматически для любого звука.

Файлы можно сложить в подпапку `rare/` внутри папки события — тогда они будут выбираться редко
(константа `RARE_FILE_WEIGHT` в `sound_manager.py`, по умолчанию доля меньше 10% от обычного файла).
Удобно для необычных вариаций звука, которые не должны звучать на каждый раз.

## Папки

- `laser_fire/` — выстрел лазерной башни
- `laser_hit/` — попадание луча по врагу (не проигрывается, если промах)
- `bullet_fire/` — выстрел пулемётной башни
- `bullet_hit/` — попадание пули по врагу (не проигрывается, если промах)
- `mortar_fire/` — выстрел (запуск снаряда) миномётной башни
- `mortar_explosion/` — взрыв миномётного снаряда при падении - это же и есть звук
  попадания мортиры, отдельной папки mortar_hit нет
- `tower_placed/` — постройка башни
- `enemy_death/` — уничтожение врага
- `base_hit/` — предупреждение, что базу атакуют (не про попадание башни по врагу); проигрывается
  не чаще раза в 7 секунд даже если по базе одновременно бьёт несколько врагов; часть вариантов
  (`rare/alarm.wav`, `rare/alarm_3.wav`) звучат редко
- `victory/` — победа
- `defeat/` — поражение

Чтобы добавить новое событие (например, апгрейд башни), заведи новую папку с тем же именем,
что и ключ события, и вызови `sound_manager.play("имя_папки")` в нужном месте UI-кода.
